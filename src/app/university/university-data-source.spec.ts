import { UniversityDataSource } from './university-data-source';

describe('UniversityDataSource', () => {
  it('should create an instance', () => {
    expect(new UniversityDataSource()).toBeTruthy();
  });
});
