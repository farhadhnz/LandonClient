import { SemesterDataSource } from './semester-data-source';

describe('SemesterDataSource', () => {
  it('should create an instance', () => {
    expect(new SemesterDataSource()).toBeTruthy();
  });
});
