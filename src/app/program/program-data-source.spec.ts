import { ProgramDataSource } from './program-data-source';

describe('ProgramDataSource', () => {
  it('should create an instance', () => {
    expect(new ProgramDataSource()).toBeTruthy();
  });
});
