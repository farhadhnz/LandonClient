import { ProvinceDataSource } from './province-data-source';

describe('ProvinceDataSource', () => {
  it('should create an instance', () => {
    expect(new ProvinceDataSource()).toBeTruthy();
  });
});
