import { CountryDataSource } from './country-data-source';

describe('CountryDataSource', () => {
  it('should create an instance', () => {
    expect(new CountryDataSource()).toBeTruthy();
  });
});
